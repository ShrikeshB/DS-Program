#include <stdio.h>
typedef struct
{
    int data;
    struct node *next;
} node;
node *front = NULL;
node *rear = NULL;

void enqueue(value)
{
    node *newNode = (node *)malloc(sizeof(node));
}
int main()
{

    return 0;
}